[wal]
enabled = false
fsync = false
buffered = true
buffer_size = 8388608
dir = "../data/wal/"
flush_each_write = false
fsync_every_n = 50000

[engine]
flush_threshold =8000
data_dir = "../data/cols"
index_dir = "../data/index/"
shard_count = 16
event_per_zone = 2000
compaction_threshold = 1000
compaction_interval = 1800
sys_io_threshold = 1000
max_inflight_passives = 128


[schema]
def_dir="../data/schema/"

[server]
socket_path = "/tmp/sneldb.sock"
log_level = "error"
output_format = "json"
tcp_addr = "127.0.0.1:7171"
http_addr = "127.0.0.1:8085"
auth_token = "mysecrettoken"

[playground]
enabled = false
allow_unauthenticated = false

[logging]
log_dir = "../data/logs"
stdout_level = "error"
file_level = "error"